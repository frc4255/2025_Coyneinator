package frc.robot.subsystems;

/**
 * Temporary real hardware implementation placeholder. Populate with the actual motor and sensor
 * wiring once the mechanism is ready.
 */
public class GroundIntakeIOReal implements GroundIntakeIO {
    @Override
    public void stop() {
        // Intentionally left blank while the mechanism is under development.
    }
}
